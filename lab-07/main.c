#include <stdio.h>
#include "header/folder.h"

int main() {
    FOLDER folder1={"galfimappa",
                    50,
                    hazi
    };
    kiirFolder(folder1);
    FOLDER folderketto;
    freopen("folder2.txt","r",stdin);
    beolvasFolder(&folderketto);
    kiirFolder(folderketto);
    return 0;
}
